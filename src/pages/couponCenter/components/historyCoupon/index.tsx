import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function index() {
    return (
        <View style={{ flex: 1, backgroundColor: 'red' }}>
            <Text>inhistoryCoupondex</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
