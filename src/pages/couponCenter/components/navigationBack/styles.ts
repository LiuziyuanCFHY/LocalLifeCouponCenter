import { EStyleUtil } from '@/utils/EStyleUtil';

export const styles = EStyleUtil.create({
    container: {
        width: 32,
        height: 32,
        justifyContent: 'center',
        alignItems: 'center',
        marginLeft: 11,
    },
    iconStyle: {
        tintColor: '#0E0E0E',
    },
    navBarContainerNew: {
        width: 24,
        height: 24,
        marginRight: 16,
        justifyContent: 'center',
        alignItems: 'center',
    },
});
