export const NO_MORE = 0;

export enum CouponCenterRouteName {
    CouponCenter = 'couponCenter',
    HistoryCoupon = 'historyCoupon',
}

export enum CouponCenterFetchType {
    CouponCenter = 1,
    HistoryCoupon = 2,
}

export const CouponPageSize = 20;

export enum CouponBusiness {
    LocalLife = 1, //本地生活
}
